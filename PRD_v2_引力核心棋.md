# Nexus · 引力核心棋 — 规则创新重设计 PRD

> 版本：v2.0 | 作者：Alice (产品经理) | 日期：2025-06-05

---

## 1. 项目信息

| 属性 | 值 |
|------|-----|
| **语言** | 中文 |
| **技术栈** | Vite + React + MUI + Tailwind CSS |
| **项目名称** | `nexus_chess`（保留） |
| **原始需求** | 重新设计 Nexus 核心棋规则，使其完全原创、与任何现有棋类明显差异化 |

---

## 2. 游戏名称

**Nexus · 引力核心棋**（Gravitational Core Chess）

核心隐喻：棋盘是一个**引力场**，每个棋子通过其「可到达范围」产生引力控制区。被敌方引力场覆盖的棋子将被**锁定**，无法移动。核心（Nexus）是唯一能穿透引力场的存在，目标是率先将核心送达棋盘中央。

---

## 3. 棋盘设计

### 3.1 基础规格

保留 **7×7 网格**（49 格），行列编号如下：

```
     a   b   c   d   e   f   g
   ┌───┬───┬───┬───┬───┬───┬───┐
 7 │   │   │   │   │   │   │   │ 7
   ├───┼───┼───┼───┼───┼───┼───┤
 6 │   │   │   │   │   │   │   │ 6
   ├───┼───┼───┼───┼───┼───┼───┤
 5 │   │   │   │   │   │   │   │ 5
   ├───┼───┼───┼───┼───┼───┼───┤
 4 │   │   │   │ ⊙ │   │   │   │ 4
   ├───┼───┼───┼───┼───┼───┼───┤
 3 │   │   │   │   │   │   │   │ 3
   ├───┼───┼───┼───┼───┼───┼───┤
 2 │   │   │   │   │   │   │   │ 2
   ├───┼───┼───┼───┼───┼───┼───┤
 1 │   │   │   │   │   │   │   │ 1
   └───┴───┴───┴───┴───┴───┴───┘
     a   b   c   d   e   f   g
```

- **d4** 为中心格，标记为「⊚ 核心点」（Nexus Point）
- 中心格**永远不属于任何一方的引力控制区**——它是中立的圣域

### 3.2 初始布局

**白方**（下方，第 1-2 行）：

```
     a   b   c   d   e   f   g
 2 │ ○ │   │ ○ │   │ ○ │   │ ○ │    ← 4 个 流子 (Flux)
 1 │   │ ■ │   │ ★ │   │ ■ │   │    ← 2 个 锚点 (Anchor) + 1 个 核心 (Core)
```

**黑方**（上方，第 6-7 行）：

```
     a   b   c   d   e   f   g
 7 │   │ ■ │   │ ★ │   │ ■ │   │    ← 2 个 锚点 + 1 个 核心
 6 │ ○ │   │ ○ │   │ ○ │   │ ○ │    ← 4 个 流子
```

- 白方先行
- 双方布局沿中心 **180° 旋转对称**

---

## 4. 棋子设计

每方 **7 枚棋子**，分为 3 种类型：

| 棋子 | 符号 | 数量 | 移动能力 | 引力控制范围 |
|------|------|------|----------|--------------|
| **核心** (Nexus) | ★ | 1 | 走 1 步（8 方向） | 1 步内 8 格 |
| **锚点** (Anchor) | ■ | 3 | 走 1-2 步（4 正交方向） | 2 步内正交 4 方向可达格 |
| **流子** (Flux) | ○ | 3 | 跳 2 步（8 方向） | 2 步距离的 8 个格子 |

### 4.1 各子详解

**核心 (Nexus) ★**
- 移动：正交或对角线走 1 格
- 引力控制：周围一圈 8 格（最大）
- **无视引力锁定**：核心永远可以在任何情况下移动（即使站在敌方控制区）。核心是唯一免疫引力锁定的棋子。
- **不可被消灭**：核心永远不会离开棋盘

**锚点 (Anchor) ■**
- 移动：正交方向走 1 或 2 格（不可越子）
- 引力控制：正交 4 方向上 2 步内的所有格子（共最多 8 格）
- **稳固特性**：锚点产生的控制区**不会被敌方引力覆盖**——锚点控制的格子即使在敌方引力范围内，控制权仍然归属锚点方（双方同时控制 = 该格成为「争议区」，对双方棋子均可移动）

**流子 (Flux) ○**
- 移动：跳跃到距离恰好 2 步的任意格子（8 方向，可越子）
- 引力控制：距离恰好 2 步的 8 个格子（跳跃目标）
- **流动特性**：流子是唯一可以**越过其他棋子**移动的棋子（跳跃式移动）

---

## 5. 核心机制：引力控制与锁定

> 这是本游戏**最核心的创新**，替代了传统棋类的「吃子」机制。

### 5.1 引力控制区（Control Zone）

每个棋子控制它能**一步到达**的格子。一方的控制区 = 该方所有棋子控制范围的**并集**。

### 5.2 引力锁定（Gravitational Lock）

> ⚠️ **核心规则**：如果一枚棋子站在**敌方引力控制区**内，则该棋子**不能移动**。

- 例外：**核心 (★) 免疫引力锁定**——核心永远可以移动
- 争议区（双方同时控制的格子）：双方棋子均可在该格上移动（互不锁定）
- 中心格 d4：永远不属于任何控制区，任何棋子站在 d4 上都不会被锁定

### 5.3 关键推论

1. 你无法「吃」掉敌方棋子——只能通过引力**困住**它们
2. 要推进棋子，必须先清除路径上的敌方引力覆盖
3. 锚点的「稳固」特性使其成为防御和推进的支点
4. 流子擅长快速改变控制区形状，但自身脆弱（容易被锁定）
5. 核心虽然免疫锁定，但孤军深入会被包围困死（无法再推进其他棋子）

---

## 6. 获胜条件

仅 **1 种获胜方式**，清晰直接：

> **将你的核心 (★) 走到棋盘中心 d4，并在下一回合开始时核心仍位于 d4。**

具体判定：
1. 白方将 ★ 走到 d4 → 轮到黑方 → 黑方必须在其回合内将白方 ★ 赶出 d4
2. 如果黑方无法赶出（白方 ★ 仍在 d4）→ 白方**立即获胜**
3. 同理，黑方 ★ 走到 d4 → 轮到白方 → 白方无法赶出 → 黑方获胜

**注意**：中心格 d4 免疫引力控制，因此在 d4 的核心**不能被引力锁定**。要赶走 d4 上的敌方核心，必须用一个棋子**物理占据 d4** 将其推开——但 d4 同时只能站一个棋子，所以赶走核心的唯一方法是让核心**自己移动走**（被威胁逼迫）或通过特殊规则。

### 6.1 修正：中心争夺细则

由于 d4 免疫引力且只能站一个棋子，需要「推离」机制：

> **推离规则**：如果你的棋子站在 d4 的相邻格（8 个邻居），且该格在你的引力控制区内，则你可以消耗一次移动，将 d4 上的敌方核心**推离**到任意一个**不在你控制区内**的空相邻格。如果不存在这样的空相邻格，敌方核心**被引力囚禁**——无法被推离，你无法阻止对方获胜。

这意味着：
- 要阻止敌方核心在 d4 获胜，你需要至少控制 d4 周围的部分格子
- 要保护自己的核心在 d4，你需要清除 d4 周围的敌方控制

---

## 7. 特殊规则

### 7.1 无「吃子」
本游戏**不存在吃子**。棋子永远不会被移出棋盘。战斗通过引力锁定和位置争夺进行。

### 7.2 无王手/将军
没有「将死」概念。游戏只通过中心占领获胜。

### 7.3 和棋条件
如果连续 **20 回合**没有任何棋子进入或离开 d4 的相邻区域（即 d4 周围 8 格），判定为**和棋**。

### 7.4 不可移动回合
如果一方所有可移动棋子都被引力锁定（且核心已在无法改善的位置），该方**跳过回合**。连续 3 次跳过 → 判负。

---

## 8. 规则简明卡片（3 分钟讲清楚）

```
┌─────────────────────────────────────────────────────────┐
│                 Nexus · 引力核心棋                        │
│                    规则简明卡片                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  🎯 目标：把你的 ★（核心）走到中心 d4，保持到下一回合      │
│                                                         │
│  📦 棋子（每方 7 枚）：                                   │
│    ★ 核心 ×1 — 走 1 格（8方向），免疫引力锁定              │
│    ■ 锚点 ×3 — 走 1-2 格（正交），控制区不会被敌方覆盖     │
│    ○ 流子 ×3 — 跳 2 格（8方向），可越子                   │
│                                                         │
│  🔑 核心规则 — 引力锁定：                                 │
│    你控制的格子 = 你棋子一步能到达的所有格子                │
│    敌方棋子站在你的控制区 → 它不能动！                      │
│    例外：★ 免疫锁定，d4 中立                              │
│                                                         │
│  ⚔️ 推离：                                              │
│    敌方 ★ 站 d4 → 用你控制 d4 相邻格的能力推开它           │
│                                                         │
│  🚫 没有吃子 | 没有将军 | 没有王手                         │
│                                                         │
│  ⏱ 对局时长：10-25 分钟                                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 9. 与国际象棋的差异对比表

| 维度 | 国际象棋 | Nexus · 引力核心棋 |
|------|----------|---------------------|
| **棋盘** | 8×8（64 格） | 7×7（49 格） |
| **每方子数** | 16 | 7 |
| **棋子类型** | 6 种（王后车象马兵） | 3 种（核心/锚点/流子） |
| **核心交互** | 吃子——移动到敌方棋子位置将其移除 | 引力锁定——控制区使敌方棋子无法移动 |
| **胜负条件** | 将死对方王 | 核心占据中心并保持一回合 |
| **王的安全性** | 被将军必须应将，被将死则输 | 核心免疫引力锁定，可自由行动 |
| **棋子可否被消灭** | 是（除王） | 否——所有棋子永远留在棋盘上 |
| **将军/王手** | 核心机制 | 不存在 |
| **特殊走法** | 王车易位、吃过路兵、升变 | 推离（将核心推离中心） |
| **和棋方式** | 逼和、三次重复、50 回合规则等 | 20 回合无中心争夺 + 连续 3 次跳过判负 |
| **策略核心** | 子力优势 + 空间 + 将杀模式 | 引力控制区争夺 + 核心推进路径规划 |
| **典型时长** | 10-60 分钟（快棋） | 10-25 分钟 |
| **学习曲线** | 易学难精（6 种子走法需记忆） | 极易学（3 种子 + 1 条核心规则） |
| **开局理论** | 数百年积累，海量定式 | 完全空白，待探索 |

---

## 10. 技术规范（开发参考）

### 10.1 需求优先级

| 优先级 | 需求 | 说明 |
|--------|------|------|
| **P0** | 引力控制区计算引擎 | 核心机制，所有逻辑的基础 |
| **P0** | 引力锁定判定 | 决定棋子可否移动 |
| **P0** | 三种棋子移动规则 | 核心/锚点/流子的走法生成 |
| **P0** | 中心占领与推离逻辑 | 胜负判定 |
| **P1** | 跳过回合检测 | 不可移动时的处理 |
| **P1** | 和棋条件检测 | 20 回合无中心争夺 |
| **P2** | 走法高亮与引力区可视化 | UI 辅助 |
| **P2** | FEN 扩展格式 | 需新增引力控制区信息 |

### 10.2 数据结构适配

现有 NNUE HalfKP 特征提取（`features.py`）使用：

```python
piece_code: 0=空, 1=白核心, 2=白锚点, 3=白流子, 5=黑核心, 6=黑锚点, 7=黑流子
```

**改动**：piece_code 含义变化（从 Core/Sentinel/Scout 变为 Core/Anchor/Flux），但编码结构**不变**——只需更新映射表。HalfKP 特征维度仍为 ~20,480。

### 10.3 走法表示

走法格式保持 `{from}{to}` 代数记法（如 `d2d4`、`○c3e5`），向前兼容现有协议。

### 10.4 Open Questions

1. **锚点数量**：3 个锚点是否过多？考虑减少为 2 个锚点 + 4 个流子？
2. **推离半径**：推离 d4 上的核心时，应允许推到多远？仅相邻格还是任意空位？
3. **核心 CD**：核心被推离后是否需要冷却时间（如 1 回合内不可再次进入 d4）？
4. **流子跳跃阻塞**：流子跳跃时是否可被中途的锚点「引力拦截」？

---

## 11. UI 设计草案

### 11.1 棋盘渲染

- 7×7 深色棋盘，d4 中心格采用**金色光晕**突出显示
- 白方棋子白色/浅蓝，黑方棋子深灰/暗红
- **引力控制区叠加层**：选中棋子时，高亮其控制范围（半透明色块）
- **被锁定棋子**：显示红色边框或锁链图标
- **可推离提示**：当敌方核心在 d4 时，显示可推离的相邻格

### 11.2 棋盘状态栏

```
[白方]  ★ b1  ■ a1 c1  ○ a2 c2 e2 g2    ← 棋子位置摘要
控制区: 23 格           ← 当前方控制格数
锁定敌方: ○ d6 ■ e7     ← 当前被锁定的敌方棋子
```

---

*本文档为 Nexus 引力核心棋 v2.0 完整规则设计。所有机制均为原创设计，不与任何现有棋类雷同。*
